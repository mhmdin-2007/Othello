#ifndef ENUMS_HPP
#define ENUMS_HPP

enum class Color : char {
    BLACK = 'B',
    WHITE = 'W',
    EMPTY = '.',
    DRAW = 'D'
};

enum class Difficulty : int { 
    EASY = 1, 
    MEDIUM = 2, 
    HARD = 3 
};

#endif
